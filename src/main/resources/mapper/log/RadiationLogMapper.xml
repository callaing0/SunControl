<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.core.repository.log.RadiationLogRepository">
    <!--  일사량데이터 저장  -->
    <!--  TODO : 건수가 많아지면 BATCH로 전환해야 함  -->
    <insert id="saveAll" parameterType="com.suncontrol.core.entity.log.RadiationLog">
        INSERT INTO radiation_log (
        plant_id, base_time, gti, gti_instance
        ) VALUES
        <foreach collection="list" item="item">
            (#{item.plantId}, #{item.baseTime}, #{item.gti}, #{item.gtiInstance})
        </foreach>
        ON DUPLICATE KEY UPDATE
        gti = VALUES(gti),
        gti_instance = VALUES(gti_instance),
        updated_at = NOW()
    </insert>
</mapper>